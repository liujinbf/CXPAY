name: Runner Smoke Diagnostic

on:
  pull_request:

permissions:
  contents: read

jobs:
  runner-smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Prove runner starts
        shell: bash
        run: |
          echo "runner-started"
          uname -a
